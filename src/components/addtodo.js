import React, {Fragment} from 'react'
let todoInput;

function addtodo(props) {
    return (
        <Fragment>
            <input/>
            <button>Add</button>
        </Fragment>
    )
}

export default addtodo
